(define (+ a b)
  (if (= a 0)
	b
	(inc(+ (dec a) b))))

(define (+ a b)
  (if (= a 0)
	b
	(+ (dec a) (inc b))))

The first procedure is linear recursive due to it shape and stack usage.
The second is linear iterative since it stores it's values.
